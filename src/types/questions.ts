export interface QuestionType {
  id: string;
  id_site: string;
  question: string;
  answer: string;
}

export interface RawQuestionData {
  id: string;
  id_site: string;
  question: string;
  answer: string;
}
